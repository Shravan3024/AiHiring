require("dotenv").config({ path: ".env.test" });

// Global setup for tests (e.g., seeding a test database)
beforeAll(async () => {
  // Initialization logic if needed
});

afterAll(async () => {
  // Cleanup logic if needed
});
